import 'package:flutter/material.dart';

/// Semantic colors from Dotto Design System
class SemanticColors {
  // Text Colors
  static const textPrimary = Color(0xFF3C373C);
  static const textSecondary = Color(0xBF3C373C); // 0.75 opacity
  static const textPlaceholder = Color(0x803C373C); // 0.5 opacity
  static const textDisabled = Color(0x613C373C); // 0.38 opacity
  static const textLink = Color(0xFF0089F0);
  static const textOnColor = Color(0xFFFFFFFF);
  static const accentPrimary = Color(0xFF990000);

  // Background Colors
  static const backgroundPrimary = Color(0xFFF8F8F8);
  static const backgroundSecondary = Color(0xFFFFFFFF);
  static const backgroundDisabled = Color(0x263C373C); // 0.15 opacity
  static const backgroundModal = Color(0x5C000000); // 0.36 opacity

  // State Colors
  static const stateActive = Color(0x663C373C); // 0.4 opacity
  static const stateDisabled = Color(0x423C373C); // 0.26 opacity
  static const stateHover = Color(0x1A3C373C); // 0.1 opacity

  // Border Colors
  static const borderDivider = Color(0x1F000000); // 0.12 opacity
  static const borderOutline = Color(0xD9CCCCCC); // 0.85 opacity
  static const borderOutlineOnColor = Color(0xFFFFFFFF);

  // Primary Colors
  static const primaryMain = Color(0xFF990000);
  static const primaryLight = Color(0xFFD87C7C);
  static const primaryDark = Color(0xFF650000);

  // Expressive Colors - Success
  static const successMain = Color(0xFF4CAF50);
  static const successLight = Color(0xFFCDEFCF);
  static const successDark = Color(0xFF006504);

  // Expressive Colors - Info
  static const infoMain = Color(0xFF0089F0);
  static const infoLight = Color(0xFFE3F3FF);
  static const infoDark = Color(0xFF0060A8);

  // Expressive Colors - Warning
  static const warningMain = Color(0xFFFF9500);
  static const warningLight = Color(0xFFFFF1DD);
  static const warningDark = Color(0xFFBA6D00);

  // Expressive Colors - Error
  static const errorMain = Color(0xFFF51F1F);
  static const errorLight = Color(0xFFFFE2E2);
  static const errorDark = Color(0xFFB91212);
}
